# Local runtime caches and session hints (never the source of truth)
.atl/
.ospec/

# SDD planning artifacts — kept local, not published to the repo
openspec/

# Build outputs and coverage
dist/
coverage/

# Node
node_modules/
npm-debug.log*

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
*.swp
