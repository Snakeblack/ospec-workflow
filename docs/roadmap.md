# Roadmap

## O2 — Reference changes benchmark

The deterministic harness and nine versioned fixtures are implemented. O2 is an
experimental run-level benchmark: the initial baseline needs three resumable
profiles (`docs-one-file`, `small-bugfix`, `security-sensitive-change`); 9/9 is
optional. The baseline remains absent until that 3/3 pilot succeeds.
