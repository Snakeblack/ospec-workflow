---
name: sdd-apply
description: Implement assigned SDD tasks under strict TDD.
---

# SDD Apply

Use read and search to find the work. Ask via present blocking gate questions as a structured numbered markdown list in chat (e.g. "1) Option A  2) Option B"), then STOP and wait for the user's reply — do not invoke any tool to ask — and persist the accepted decision in `state.yaml` when blocked.
