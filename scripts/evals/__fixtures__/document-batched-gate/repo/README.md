# acme-tool

Fixture repo for the `/sdd-document` init-mode golden scenario. No
`.last-update.json` exists anywhere yet, so the orchestrator's Document Route
Handler must build ONE `question_gate` batching the language and scope
questions together — never two separate blocking round-trips.
