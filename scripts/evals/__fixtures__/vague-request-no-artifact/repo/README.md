# acme-tool

A small internal CLI tool. This is a golden-eval fixture repo: minimal but
structurally realistic, used to exercise the orchestrator's intent-briefing
gate against an eligible new SDD request that names no target, criterion, or
scope. The gate MUST fire and no change directory MUST be created while waiting.
