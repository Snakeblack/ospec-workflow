# acme-tool

A small internal CLI tool. Fixture repo for the specific-request intent-briefing
golden: the request already names a target file, an acceptance criterion, and an
explicit out-of-scope boundary. The orchestrator MUST still emit the briefing
gate and MUST NOT create a change directory while waiting.
